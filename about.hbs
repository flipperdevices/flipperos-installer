# Third-party licenses

The FlipperOS installer binary statically links the Rust crates listed below.
The installer's own source is MIT-licensed, but because it links the
[Slint](https://slint.dev) GUI toolkit under the GNU General Public License
v3.0 (we hold no separate Slint license agreement), **the distributed binary as
a whole is licensed under GPL-3.0-only**. All sources remain publicly buildable.

This file is generated from `Cargo.lock` by `scripts/gen-third-party-licenses.sh`
— do not edit it by hand.

{{#each overview}}
- **{{{name}}}** — {{count}} crate(s)
{{/each}}

{{#each licenses}}
--------------------------------------------------------------------------------

## {{{name}}} ({{{id}}})

Used by:
{{#each used_by}}
  * {{{crate.name}}} {{{crate.version}}} ({{#if crate.repository}}{{{crate.repository}}}{{else}}https://crates.io/crates/{{{crate.name}}}{{/if}})
{{/each}}

```
{{{text}}}
```
{{/each}}
